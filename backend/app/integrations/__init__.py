"""Integrations: the same report, shaped for the places the work actually happens.

`payloads.py` turns a Report into a Teams card / Slack message / webhook body / email / Jira issues /
calendar file. `deliver.py` is the only place that sends anything.
"""
